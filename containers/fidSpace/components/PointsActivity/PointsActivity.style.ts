import styled from "styled-components";
import { getSpacing } from "~styles/styleSheet";

export const StyledPointsActivityContainer = styled.div`
  display: flex;
  flex-direction: column;
  gap: ${getSpacing(2)};
`;
